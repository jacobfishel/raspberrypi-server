# This file is for random coding to figure stuff out if needed

# data = [{'filename': '1.jpg', 'bboxes': [[1, 1, 1, 1, 1]]}, {'filename': '2.jpg', 'bboxes': [[2, 2, 2, 2, 2], [1, 2, 3, 5, ]]}, {'filename': '3.jpg', 'bboxes': [[3, 3, 3, 3, 3]]}]

# print(data)
# for dict in data:
#   print(f"dict[filename] = {dict['filename']}")
#   print(f"dict[bboxes] = {dict['bboxes']}")
#   for bbox in dict['bboxes']:
#     print(f"bbox: {bbox}")

# s = 'data/WIDER/images/0--1.jpg'
# print(s.replace("data/WIDER", ""))

data = [1, 2, 3, 4]
print(" ".join(map(str, data)))
print(str(data))