# Loads a trained model, runs it on validation/test images, visualizes results

# Also after each detection, send that data to the database on the pi.
  # This means no TCP server, switching to HTTP since easier to read